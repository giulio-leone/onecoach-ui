/**
 * Re-export from shared location to maintain backward compatibility.
 * @see ../shared/settings-panel.tsx for implementation
 */
export {
  AgentSettingsPanel,
  PROVIDER_OPTIONS,
  MODEL_OPTIONS,
  DEFAULT_MODELS,
  useAIModels,
} from '../shared/settings-panel';

export type { NonRouterProvider } from '../shared/settings-panel';
