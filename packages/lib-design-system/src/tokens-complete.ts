/**
 * @deprecated Use `tokens.ts` directly — this file is kept for backward compatibility
 * and re-exports the unified token system.
 */
export { designTokens, type DesignTokens, type ColorMode } from './tokens';

